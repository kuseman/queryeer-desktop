<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.queryeer.backend</groupId>
    <artifactId>queryeer-backend</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>queryeer-backend</name>
    <description>Standalone backend reactor for Queryeer Desktop</description>

    <properties>
        <maven.compiler.release>25</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <checkstyle.plugin.version>3.6.0</checkstyle.plugin.version>
        <checkstyle.version>10.17.0</checkstyle.version>
        <spotless.plugin.version>2.43.0</spotless.plugin.version>
        <surefire.plugin.version>3.2.5</surefire.plugin.version>
        <jackson.version>2.18.2</jackson.version>
        <junit.jupiter.version>6.0.3</junit.jupiter.version>
        <mockito.core.version>5.23.0</mockito.core.version>
        <maven.dependency.plugin.version>3.8.1</maven.dependency.plugin.version>
        <maven.assembly.plugin.version>3.7.1</maven.assembly.plugin.version>
        <checkstyle.skip>false</checkstyle.skip>
        <queryeer.config.root>${project.basedir}</queryeer.config.root>
    </properties>

    <profiles>
        <profile>
            <id>checkstyle-enabled</id>
            <activation>
                <property>
                    <name>!checkstyleSkip</name>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-checkstyle-plugin</artifactId>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>config-root-in-parent-of-module</id>
            <activation>
                <file>
                    <exists>${project.basedir}/../eclipse-importorder.cfg</exists>
                </file>
            </activation>
            <properties>
                <queryeer.config.root>${project.basedir}/..</queryeer.config.root>
            </properties>
        </profile>
    </profiles>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-annotations</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${junit.jupiter.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-core</artifactId>
                <version>${mockito.core.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <modules>
        <module>backend-api</module>
        <module>backend-contract</module>
        <module>backend-core</module>
        <module>backend-lib-queryengine-jdbc-foundation</module>
        <module>backend-lib-queryengine-sql-parser</module>
        <module>backend-transport-stdio</module>
        <module>backend-runner</module>
        <module>backend-plugin-payloadbuilder</module>
        <module>backend-plugin-jdbc</module>
        <module>backend-plugin-dialect-sqlserver</module>
    </modules>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.15.0</version>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${surefire.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-dependency-plugin</artifactId>
                    <version>${maven.dependency.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-assembly-plugin</artifactId>
                    <version>${maven.assembly.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-checkstyle-plugin</artifactId>
                    <version>${checkstyle.plugin.version}</version>
                    <dependencies>
                        <dependency>
                            <groupId>com.puppycrawl.tools</groupId>
                            <artifactId>checkstyle</artifactId>
                            <version>${checkstyle.version}</version>
                        </dependency>
                    </dependencies>
                    <configuration>
                        <consoleOutput>true</consoleOutput>
                        <failsOnError>true</failsOnError>
                        <linkXRef>false</linkXRef>
                    </configuration>
                    <executions>
                        <execution>
                            <id>validate-checkstyle-main</id>
                            <phase>validate</phase>
                            <goals>
                                <goal>check</goal>
                            </goals>
                            <configuration>
                                <sourceDirectories>
                                    <sourceDirectory>src/main/</sourceDirectory>
                                </sourceDirectories>
                                <includeResources>true</includeResources>
                                <includeTestResources>false</includeTestResources>
                                <configLocation>${queryeer.config.root}/checkstyle.xml</configLocation>
                                <skip>${checkstyle.skip}</skip>
                            </configuration>
                        </execution>
                        <execution>
                            <id>validate-checkstyle-test</id>
                            <phase>validate</phase>
                            <goals>
                                <goal>check</goal>
                            </goals>
                            <configuration>
                                <sourceDirectories>
                                    <sourceDirectory>src/test/</sourceDirectory>
                                </sourceDirectories>
                                <includeResources>true</includeResources>
                                <includeTestResources>true</includeTestResources>
                                <configLocation>${queryeer.config.root}/checkstyle-test.xml</configLocation>
                                <skip>${checkstyle.skip}</skip>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>com.diffplug.spotless</groupId>
                    <artifactId>spotless-maven-plugin</artifactId>
                    <version>${spotless.plugin.version}</version>
                    <configuration>
                        <java>
                            <removeUnusedImports/>
                            <importOrder>
                                <file>${queryeer.config.root}/eclipse-importorder.cfg</file>
                            </importOrder>
                            <eclipse>
                                <file>${queryeer.config.root}/eclipse-code-formatter.xml</file>
                            </eclipse>
                        </java>
                    </configuration>
                    <executions>
                        <execution>
                            <id>spotless-check</id>
                            <phase>validate</phase>
                            <goals>
                                <goal>check</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
